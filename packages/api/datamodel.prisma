type User {
  id: ID! @id
  name: String!
}

type Metric {
  id: ID! @id
  name: String!
  dataPoints: [DataPoint] @relation(name: "MetricToDataPoint")
}

type DataPoint {
  id: ID! @id
  dateTime: DateTime
  metric: Metric! @relation(name: "MetricToDataPoint")
}
